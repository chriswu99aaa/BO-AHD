defaults:
  - _self_
  - problem: tsp_constructive
  - llm_client: openai #dashscope, openai
  - override hydra/output: local

hydra:
  job:
    name: ${problem.problem_name}-${problem.problem_type}
    chdir: True

# The chosen algorithm [bo_local, bo_baseline,mcts-ahd]
algorithm: bo_baseline

# Main GA loop parameters
max_fe: 200 # maximum number of function evaluations
pop_size: 20 # population size for GA
init_pop_size: 4 # initial population size for GA
timeout: 60 # timeout for evaluation of a single heuristic

# Main BO loop parameters

n_init: 10 # number of initial random samples
n_iters: 300 # number of iterations for BO
num_samples: 5

max_train_size: 300

# GP fitting parameters
gp_fit_sample_size: 250

# Continuation parameters
exp_use_continue: False # whether to use continuation
exp_continue_path: "/Users/chriswu/GitCode/BoTree-AHD/outputs/tsp_constructive-constructive/baseline_no_bo/population_generation_200.json" # path to the continuation file

lambda_value: 0.7 # the weight between train_length and gap in the objective function
parent_sample_size: 3 # number of parents to sample for expansion in BO